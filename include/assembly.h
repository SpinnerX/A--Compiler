#pragma once


namespace A_Compiler{
	/*
	 *
	 * @function generateArm64Asm
	 * @note generates ARM64 asm from the ast.
	 * @note am thinking of passing the ast through here
	 * @note also didnt add assembly.cc to src/asm yet, but should add that if not there already.
	 * */
	void generateArm64Asm();
};
